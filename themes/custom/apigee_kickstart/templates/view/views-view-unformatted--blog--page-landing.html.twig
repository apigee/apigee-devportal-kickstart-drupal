{#
/**
 * @file
 * Template for the Blog landing page view.
 */
#}

{% if title %}
  <h3>{{ title }}</h3>
{% endif %}
{% for row in rows %}
  {%
    set row_classes = [
    default_row_class ? 'views-row',
  ]
  %}
  <div{{ row.attributes.addClass(row_classes) }}>
    {{- row.content -}}
  </div>
  <div class="my-4 border-light border-bottom d-none d-sm-block"></div>
{% endfor %}
