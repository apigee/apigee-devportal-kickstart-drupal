{#
/**
 * @file
 * Default theme implementation to status_property_element.
 *
 * Available variables:
 * - element: Element that will be rendered.
 * - element['value'] : the status string.
 *
 * @ingroup themeable
 */
#}
{% set statuses = {
  approved: 'success',
  revoked: 'danger',
} %}

{% include '@radix/badge/badge.twig' with {
  type: statuses[element.value|trim]|default('info'),
  content: element.value|capitalize,
} %}
